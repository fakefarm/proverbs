<%= form_for(@kid) do |f| %>
  <% if @kid.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@kid.errors.count, "error") %> prohibited this kid from being saved:</h2>

      <ul>
      <% @kid.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.hidden_field :parent_id, value: current_user.parent.id %>
  </div>

  <div class="field">
    <%= f.text_field :name, placeholder: 'Oscar' %>
  </div>
  <div class="actions">
    <%= f.submit "Teach me wisdom, #{current_user.parent.name}"%>
  </div>
<% end %>
